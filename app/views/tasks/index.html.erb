<!--? Nasser:  will be uncomment soon-->
<!-- 
<% if current_user %>
  <div class="mt-4" >
    <%= render "form", task: @task %>
  </div>
  <% if current_user.tasks.any? %>
    <div id="tasks">
      <div class="mx-auto max-w-2xl text-center">
        <h2 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-3xl"> <%=t("tasks.index.ai_marketing_posts")%></h2>
      </div>
      <% if flash.now[:notice].present? %>
        <%= render 'shared/flash' %>
      <% end %>
      <div class="mt-4 border-t border-gray-900/10 pt-8 md:flex md:items-center md:justify-between">
        <div class="px-5" data-controller="tasks">
          <%= render @tasks %>
        </div>
      </div>
    </div>
  <% end %>
<% end %>
<% if !current_user %>
  <div class="px-6 py-24 sm:px-6 sm:py-32 lg:px-8">
    <div class="mx-auto max-w-2xl text-center">
      <h2 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl"><%=t("tasks.index.revolutionize_your_real_estate_marketing")%><br>
      </h2>
      <p class="mx-auto mt-6 max-w-xl text-lg leading-8 text-gray-600"><%=t("tasks.index.revolutionize_your_real_estate_marketing_description")%></p>
      <div class="mt-10 flex items-center justify-center gap-x-6">
        <%= link_to t("tasks.index.try_thrb_ai_free"), users_sign_in_path ,  class: "rounded-md bg-cyan-600	 px-3.5 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-cyan-500	 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cyan-600" %>
      </div>
    </div>
  </div>
<% end %>
</div>

-->
<div class="isolate p-4 sm:p-6 md:p-8 lg:p-10 xl:p-12">
  <div class="mt-4" >
    <%= render "form", task: @task %>
  </div>
  <div id="tasks">
    <div class="mx-auto max-w-2xl text-center">
      <h2 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-3xl"> <%=t("tasks.index.ai_marketing_posts")%></h2>
    </div>
    <% if flash.now[:notice].present? %>
      <%= render 'shared/flash' %>
    <% end %>
    <div class="mt-4 border-t border-gray-900/10 pt-8 md:flex md:items-center md:justify-between">
      <div class="px-5" data-controller="tasks">
        <%= render @tasks %>
      </div>
    </div>
  </div>
</div>